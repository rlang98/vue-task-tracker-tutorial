import { createApp } from 'vue'
import App from './App.vue'

//Entry point for Vue
//create app, pasing in App from vue, mounting it to the div with the Id of app in index.html
createApp(App).mount('#app')
